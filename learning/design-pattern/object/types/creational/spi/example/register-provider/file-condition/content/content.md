# Content

- the file should contain the fully qualified names of the service provider implementations, one per line;
- each line should be the name of a class that implements the service interface;
- these classes must be available on the classpath at runtime.

**Example:**

```text
com.example.MyServiceImpl1
com.example.MyServiceImpl2
```
