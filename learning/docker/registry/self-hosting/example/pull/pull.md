# Pull an Image

Pull the image back to verify it works:

```bash
docker pull localhost:5000/my-image
```

**Command Overview:**

See the instructions to pull an image within the [image chapter](../../../../image/common-command/push-pull/push_pull.md).
