# Intent
- the **Facade Pattern** was created to **simplify access to complex systems** by introducing a central object (**Facade**) that provides a unified interface to a set of related components;
- this improves **maintainability** and **reduces coupling** by **hiding the subsystem’s complexity** behind a single entry point.
