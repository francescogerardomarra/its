# File Location

- the file must be located in the `META-INF/services/` directory of the JAR file or classpath;
- this is where `ServiceLoader` at runtime looks for service provider configuration files.
