# Dependencies
A dependency is any object that another object needs to function properly.

**Think of it like this:**

If you have a `Car`, it depends on an `Engine` to run. So the `Car` has a dependency on the `Engine`.

Therefore we need to **inject** an `Engine` into the `Car` to make it work!